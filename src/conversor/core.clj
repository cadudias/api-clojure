(ns conversor.core
  (:gen-class))

;; -main = método estatico
(defn -main
  "I don't do a whole lot ... yet."
  [& args] ;; [& args] & = quantidade indefinida de argumentos
  (println "Hello, World!"))
